spruce:
- base: ci/meta.yml
  prune:
  - meta
  merge:
  - with_in: ci/
    regexp: ".*yml"
  - with_in: ci/jobs/
  to: aviator_pipeline.yml
fly:
  name: dataworks-github-config
  target: concourse
  config: aviator_pipeline.yml
  expose: true
